---
- hosts: all
  roles:
    - packages
    - gpu
    - drivers
    - development
    - security
    - customization
  become: no
  vars:
    ansible_python_interpreter: /usr/bin/python3
    gpu: amd
  pre_tasks:
    - name: Get upstream name
      shell: lsb_release -ucs
      register: lsb_name
